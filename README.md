UI: HTML/CSS/JS
Framework: Flask (both Pyhton and Flask need to be install)
Data: Jonty Airline route

Steps to install Flask:
1) Make sure Python installed (https://www.python.org/downloads/) 
2) Install flask using command 'python3 -m pip install flask' in cmd

Steps to run:
1) Run app.py
2) Open home.html through localhost 